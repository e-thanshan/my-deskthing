# pinned so this repo resolves the same way on a laptop with a company-wide registry in
# ~/.npmrc as it does in ci, where no user npmrc exists. published bundle digests depend on it.
registry=https://registry.npmjs.org/
